/**
 * Package that contains abstractions needed for storing local node
 * state information; metadata about system that runs using StoreMate,
 * not actual data stored (or metadata thereof).
 */
package com.fasterxml.storemate.store.state;
